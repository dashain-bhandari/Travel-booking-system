/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "images.unsplash.com", "plus.unsplash.com","res.cloudinary.com"],
  },
};

export default nextConfig;


